# ============================================================
# DealFlow360 - Auth Service
# ============================================================
# authenticate_user(email, password) -> User | None
# register_customer(data) -> User
# send_invite_email(email, role, token)
# accept_invite(token, password, full_name) -> User
# send_password_reset(email)
# reset_password(token, new_password)
# ============================================================
