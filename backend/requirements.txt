# DealFlow360 — Backend Dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.36
psycopg2-binary>=2.9.9        # PostgreSQL driver for SQLAlchemy
asyncpg==0.30.0               # async PostgreSQL driver
alembic==1.13.3               # database migrations
pgvector==0.3.3               # vector search (for AI/RAG if needed)

# Auth & security
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # password hashing

# Config
pydantic-settings==2.5.2

# Email (Resend)
resend==2.4.0

# AI / ML
openai==1.50.0            # LLM (swap key for Gemini/Anthropic if needed)
scikit-learn==1.5.2       # recommendation engine / anomaly detection
numpy==2.1.1
pandas==2.2.3

# Background tasks (add only if needed)
# celery==5.4.0
# redis==5.1.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2             # async test client for FastAPI

# Dev utilities
python-dotenv==1.0.1
